# The delivered lockfile resolves only with legacy peer resolution
# (@builder.io/vite-plugin-jsx-loc pins an older vite peer). Without this line
# `npm ci` fails and the Docker build never reaches the first byte.
legacy-peer-deps=true
