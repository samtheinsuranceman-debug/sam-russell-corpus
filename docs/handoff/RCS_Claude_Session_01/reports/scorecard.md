# Integration Scorecard
## Every page, ten wiring dimensions read from the code, beside value and frequency

Generated 2026-09-18 by `server/integrationScorecard.script.ts`. Wiring is computed; value and frequency are judgements from `shared/pageRatings.ts`. The gap (value − wiring) is the work list.

## Summary

| Pages | Mean wiring | At 10 | Below 5 |
|---|---|---|---|
| 115 | 3.3 | 0 | 76 |

### System-wide gaps (pages missing each dimension)

| Dimension | Weight | Pages missing |
|---|---|---|
| Routed | 1 | 0 |
| Engine declared | 1 | 71 |
| Page uses an engine | 1 | 77 |
| In the AI brain | 1.5 | 66 |
| Engine tested | 1 | 67 |
| Genome routes here | 1 | 101 |
| Cross-linked | 1 | 104 |
| On the sphere | 0.5 | 77 |
| Live external data | 1 | 104 |
| Figure trace | 1 | 112 |

Routers fetching external data: career, erosion, forgiveness, incomeLife, inheritance, integration, ltc, outsideForces, rental, taxSchedule, unasked, zip

## The rated pages, by gap

| Page | Wiring | Value | Freq | Gap | Missing |
|---|---|---|---|---|---|
| Ecological Drivers of Retirement Success `/portal/ecological-drivers` | 1 | 8 | 6 | 7.0 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Short-Term Rental Tax Eliminator `/portal/str-tax-eliminator` | 1 | 8 | 3 | 7.0 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| The Time Machine `/portal/time-machine` | 1 | 8 | 4 | 7.0 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Business Owner Planning `/portal/business-owner` | 1.5 | 8 | 3 | 6.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Estate Tax `/portal/estate-tax` | 1.5 | 8 | 3 | 6.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Social Security Optimizer `/portal/social-security` | 1.5 | 8 | 8 | 6.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Withdrawal Sequencing `/portal/withdrawal-sequencing` | 1.5 | 8 | 6 | 6.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Tax-Free Wealth Combinations `/portal/tax-combos` | 2 | 8 | 5 | 6.0 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, On the sphere, Live external data, Figure trace |
| Carrier Strength `/portal/carrier-ratings` | 1 | 7 | 4 | 6.0 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Estate Planning `/portal/estate-planning` | 1 | 7 | 6 | 6.0 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Medicare IRMAA Planner `/portal/medicare-irmaa` | 1.5 | 7 | 6 | 5.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Oil & Gas Deductions `/portal/oil-gas` | 2 | 7 | 2 | 5.0 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Cross-linked, On the sphere, Live external data, Figure trace |
| Outside Forces `/portal/outside-forces` | 3.5 | 8 | 5 | 4.5 | Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Figure trace |
| House Recycling `/portal/house-recycling` | 2.5 | 7 | 3 | 4.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Cross-linked, Live external data, Figure trace |
| The Decade Machine `/ultra-calculator` | 5 | 9 | 7 | 4.0 | In the AI brain, Genome routes here, On the sphere, Live external data, Figure trace |
| The Fact Finder `/portal/financial-assessment` | 5.5 | 9 | 9 | 3.5 | In the AI brain, Genome routes here, Live external data, Figure trace |
| What the Genome Says to Do `/portal/genome-strategies` | 5.5 | 9 | 7 | 3.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| The Sequence Planner `/portal/sequence-planner` | 5.5 | 9 | 4 | 3.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| The AI Brain Hub `/portal/ai-brain-hub` | 4.5 | 8 | 3 | 3.5 | Page uses an engine, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| The Career Ledger `/portal/career-path` | 4.5 | 8 | 4 | 3.5 | Page uses an engine, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Forgiveness Engine `/portal/forgiveness` | 4.5 | 8 | 3 | 3.5 | Page uses an engine, In the AI brain, Genome routes here, Cross-linked, Figure trace |
| The Inheritance Engine `/portal/inheritance` | 4.5 | 8 | 5 | 3.5 | Page uses an engine, In the AI brain, Genome routes here, Cross-linked, Figure trace |
| IUL vs Roth `/portal/iul-vs-roth` | 4.5 | 8 | 5 | 3.5 | Engine declared, Engine tested, Cross-linked, On the sphere, Live external data, Figure trace |
| Real Estate Mogul `/portal/real-estate-mogul` | 4.5 | 8 | 3 | 3.5 | Engine declared, Engine tested, Cross-linked, On the sphere, Live external data, Figure trace |
| Policy Loan Modeller `/portal/policy-loans` | 3.5 | 7 | 2 | 3.5 | Engine declared, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Tax-Free Income for Life `/portal/income-for-life` | 6 | 9 | 5 | 3.0 | Page uses an engine, Genome routes here, Cross-linked, Figure trace |
| Long-Term Care Planner `/portal/long-term-care` | 6 | 9 | 6 | 3.0 | Page uses an engine, Genome routes here, Cross-linked, Figure trace |
| The Wealth Genome `/portal/wealth-genome` | 6 | 9 | 8 | 3.0 | Page uses an engine, Cross-linked, Live external data, Figure trace |
| IUL Engine `/portal/iul-engine` | 5 | 8 | 4 | 3.0 | Page uses an engine, Genome routes here, Cross-linked, Live external data, Figure trace |
| Policy Cost Lab `/portal/policy-cost-lab` | 5 | 8 | 3 | 3.0 | Page uses an engine, Genome routes here, Cross-linked, Live external data, Figure trace |
| Index Strategy Comparison `/portal/index-strategies` | 4 | 7 | 3 | 3.0 | Page uses an engine, In the AI brain, Genome routes here, Cross-linked, On the sphere, Live external data |
| QBI / Section 199A Optimizer `/portal/qbi-optimizer` | 6.5 | 9 | 4 | 2.5 | Genome routes here, Cross-linked, On the sphere, Live external data |
| Wealth Genome Pairing Protocol `/portal/household-genome` | 5.5 | 8 | 5 | 2.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Infinite Banking `/portal/infinite-banking` | 5.5 | 8 | 3 | 2.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| The Thresholds `/portal/thresholds` | 5.5 | 8 | 3 | 2.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| MYGA Fixed Rates `/portal/myga-fixed-rate` | 4.5 | 7 | 5 | 2.5 | Engine declared, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| The Erosion Engine `/portal/erosion` | 7 | 9 | 7 | 2.0 | Page uses an engine, Cross-linked, Figure trace |
| Roth Conversion Ladder `/portal/roth-conversion` | 7 | 9 | 6 | 2.0 | Engine declared, Live external data, Figure trace |
| Tax Waterfall `/portal/tax-waterfall` | 6 | 8 | 8 | 2.0 | Genome routes here, Cross-linked, Live external data, Figure trace |
| Fixed Indexed Annuity Top Ten `/portal/fia-top10` | 5 | 7 | 4 | 2.0 | Engine declared, Genome routes here, Cross-linked, Live external data, Figure trace |
| Household Wealth `/portal/household-wealth` | 5 | 7 | 6 | 2.0 | Engine declared, Genome routes here, Cross-linked, Live external data, Figure trace |
| Alternative Lines of Credit for Rental Properties `/portal/alt-credit` | 6.5 | 8 | 3 | 1.5 | Cross-linked, On the sphere, Live external data, Figure trace |
| How a Figure Gets Made `/portal/how-a-figure-is-made` | 6.5 | 8 | 4 | 1.5 | Cross-linked, On the sphere, Live external data, Figure trace |
| Lifetime Guaranteed Income `/portal/lifetime-income` | 6.5 | 8 | 5 | 1.5 | Cross-linked, On the sphere, Live external data, Figure trace |
| The Five Mechanisms `/portal/mechanisms` | 6.5 | 8 | 5 | 1.5 | Genome routes here, On the sphere, Live external data, Figure trace |
| Liquidity Without the Bank `/portal/liquidity-routes` | 5.5 | 7 | 3 | 1.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Retirement Income Projection `/portal/retirement-projection` | 5.5 | 7 | 8 | 1.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| The Zip Engine `/portal/zip-engine` | 8 | 9 | 5 | 1.0 | Genome routes here, Figure trace |
| Mortgage Killer `/portal/mortgage-killer` | 7.5 | 8 | 7 | 0.5 | On the sphere, Live external data, Figure trace |
| The Mortgage Ledger `/portal/mortgage-ledger` | 8.5 | 8 | 6 | -0.5 | On the sphere, Live external data |
| Rental Enterprise `/portal/rental-enterprise` | 9 | 8 | 3 | -1.0 | Figure trace |
| Short-Term Rentals `/portal/short-term-rentals` | 8 | 7 | 3 | -1.0 | Cross-linked, Figure trace |

## Page by page

### Ecological Drivers of Retirement Success — `/portal/ecological-drivers`

**Wiring 1/10 · Value 8/10 · Frequency 6/10**

*Right page when:* Households who want to know what actually predicts retirement outcomes before they optimise a rate.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/portal/EcologicalDrivers.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/ecological-drivers in relatedPaths — the predictive layer cannot route a household here.
- Only 1 other surface link to /portal/ecological-drivers. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/ecological-drivers on the sphere in shared/sphere.ts.
- Calls trpc.clients (server/clients.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/ecological-drivers. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Genome: link from the durability factors.
- Sources: DOIs already verified; expose them on the page.

### Short-Term Rental Tax Eliminator — `/portal/str-tax-eliminator`

**Wiring 1/10 · Value 8/10 · Frequency 3/10**

*Right page when:* Owns or will buy a short-term rental and can meet material participation.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/STRTaxEliminatorPage.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/str-tax-eliminator in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/str-tax-eliminator. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/str-tax-eliminator on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/STRTaxEliminatorPage.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/str-tax-eliminator. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- STR engine: hand the revenue model across.
- Zip: pull nightly rates by zip.
- Engine: name strEngine here too.

### The Time Machine — `/portal/time-machine`

**Wiring 1/10 · Value 8/10 · Frequency 4/10**

*Right page when:* Any indexed-product decision; what the last thirty years would have paid.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/portal/TimeMachine.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/time-machine in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/time-machine. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/time-machine on the sphere in shared/sphere.ts.
- Calls trpc.clients (server/clients.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/time-machine. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Engine: name timeMachineEngine.ts; it has a test but the catalogue does not name it.
- IUL engine: link both ways.

### Business Owner Planning — `/portal/business-owner`

**Wiring 1.5/10 · Value 8/10 · Frequency 3/10**

*Right page when:* Owns a business with income over $150k.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/portal/BusinessOwnerPlanning.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/business-owner in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/business-owner. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Calls trpc.clients (server/clients.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/business-owner. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- QBI: link both ways.
- Career ledger: hand the practice figures across.
- Genome: link from the business strategies.

### Estate Tax — `/portal/estate-tax`

**Wiring 1.5/10 · Value 8/10 · Frequency 3/10**

*Right page when:* Estate over the exemption, or a state with its own.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/portal/EstateTax.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/estate-tax in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/estate-tax. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Calls trpc.estateTax (server/estateTax.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/estate-tax. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Live: exemption inflation adjustment annually.
- Test: estateTaxEngine.ts has none.

### Social Security Optimizer — `/portal/social-security`

**Wiring 1.5/10 · Value 8/10 · Frequency 8/10**

*Right page when:* Fifty-five and older, or younger with a spouse who is.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/portal/SocialSecurityOptimizer.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/social-security in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/social-security. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- No tRPC call from client/src/pages/portal/SocialSecurityOptimizer.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/social-security. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Engine: move the claiming arithmetic to shared/ and name it.
- Live: SSA COLA and bend points on a sweep.
- Genome: link from the guaranteed-income strategies.

### Withdrawal Sequencing — `/portal/withdrawal-sequencing`

**Wiring 1.5/10 · Value 8/10 · Frequency 6/10**

*Right page when:* Any household with more than one account type at retirement.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/portal/WithdrawalSequencing.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/withdrawal-sequencing in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/withdrawal-sequencing. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Calls trpc.clients (server/clients.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/withdrawal-sequencing. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Engine: name one; the page's arithmetic is inline.
- Erosion: feed the trajectory's tax odds into the sequencing.
- Provenance: trace the tax-saved figure.

### Tax-Free Wealth Combinations — `/portal/tax-combos`

**Wiring 2/10 · Value 8/10 · Frequency 5/10**

*Right page when:* Households ready to stack two or more strategies.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/portal/TaxFreeWealthCombos.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/tax-combos in relatedPaths — the predictive layer cannot route a household here.
- Place /portal/tax-combos on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/TaxFreeWealthCombos.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/tax-combos. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Engine: name one.
- Genome: strategy-fit should rank the combos.
- Planner: combos are sequences; route through it.

### Carrier Strength — `/portal/carrier-ratings`

**Wiring 1/10 · Value 7/10 · Frequency 4/10**

*Right page when:* Any purchase of a permanent policy or annuity.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/portal/CarrierRatings.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/carrier-ratings in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/carrier-ratings. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/carrier-ratings on the sphere in shared/sphere.ts.
- Calls trpc.carrierRatings (server/carrierRatings.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/carrier-ratings. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Live: A.M. Best and S&P on a sweep with dates.
- Engine: carrierRatings.ts exists and is untested.

### Estate Planning — `/portal/estate-planning`

**Wiring 1/10 · Value 7/10 · Frequency 6/10**

*Right page when:* Any household with dependants or property.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/EstatePlanningPage.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/estate-planning in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/estate-planning. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/estate-planning on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/EstatePlanningPage.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/estate-planning. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Engine: name one.
- Trusts: link both ways.
- Brain: no group carries estate planning rules beyond the tax engine.

### Medicare IRMAA Planner — `/portal/medicare-irmaa`

**Wiring 1.5/10 · Value 7/10 · Frequency 6/10**

*Right page when:* Sixty-three and older with income near a bracket edge.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/portal/MedicareIRMAA.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/medicare-irmaa in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/medicare-irmaa. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Calls trpc.clients (server/clients.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/medicare-irmaa. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Live: CMS IRMAA thresholds annually.
- Roth: link both ways with the conversion ladder.
- Engine: name it.

### Oil & Gas Deductions — `/portal/oil-gas`

**Wiring 2/10 · Value 7/10 · Frequency 2/10**

*Right page when:* W-2 over $300k with a large current-year liability and real appetite for illiquidity.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/OilGasPage.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- Only 0 other surfaces link to /portal/oil-gas. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/oil-gas on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/OilGasPage.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/oil-gas. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Engine: name one.
- Brain: no group carries the IDC rule.
- Provenance: trace the year-one deduction.

### Outside Forces — `/portal/outside-forces`

**Wiring 3.5/10 · Value 8/10 · Frequency 5/10**

*Right page when:* Anyone who wants the macro inputs behind every projection, with sources.

**To reach ten — from the code:**
- client/src/pages/portal/OutsideForces.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add server/outsideForces.ts to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports server/outsideForces.ts. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/outside-forces in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/outside-forces. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- No FigureTrace in shared/provenance.ts covers /portal/outside-forces. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Planner: appreciation and rate inputs should default from here.
- Provenance: each series is a sourced step; expose as traces.

### House Recycling — `/portal/house-recycling`

**Wiring 2.5/10 · Value 7/10 · Frequency 3/10**

*Right page when:* Owns a home with equity and wants a second without a second down payment.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- client/src/pages/portal/HouseRecyclingStrategy.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add its engine to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No server/*.test.ts imports the engine. Add one that reproduces a figure the page shows.
- Only 1 other surface link to /portal/house-recycling. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Calls trpc.clients (server/clients.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/house-recycling. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Mechanism: this is BRRRR on the primary; cross-link the dossier.
- Engine: name one.

### The Decade Machine — `/ultra-calculator`

**Wiring 5/10 · Value 9/10 · Frequency 7/10**

*Right page when:* Any household with a decade to plan and a tax return to start from.

**To reach ten — from the code:**
- Add shared/ultraEngine.ts to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No strategy in shared/genomeStrategies.ts names /ultra-calculator in relatedPaths — the predictive layer cannot route a household here.
- Place /ultra-calculator on the sphere in shared/sphere.ts.
- Calls trpc.ultra (server/ultraAI.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /ultra-calculator. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Genome: name it in the accumulation strategies' relatedPaths.
- Provenance: a FigureTrace for the decade headline.
- Live: FRED CPI for the real-dollar line.

### The Fact Finder — `/portal/financial-assessment`

**Wiring 5.5/10 · Value 9/10 · Frequency 9/10**

*Right page when:* Everyone, first — before any strategy page is worth reading.

**To reach ten — from the code:**
- Add shared/clientFactFinder.ts to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No strategy in shared/genomeStrategies.ts names /portal/financial-assessment in relatedPaths — the predictive layer cannot route a household here.
- Calls trpc.factFinder (server/factFinderRouter.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/financial-assessment. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Planner: the fact finder should emit a Situation.
- Genome: link on completion.

### What the Genome Says to Do — `/portal/genome-strategies`

**Wiring 5.5/10 · Value 9/10 · Frequency 7/10**

*Right page when:* After the genome; the page that says what to do.

**To reach ten — from the code:**
- No strategy in shared/genomeStrategies.ts names /portal/genome-strategies in relatedPaths — the predictive layer cannot route a household here.
- Only 1 other surface link to /portal/genome-strategies. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/genome-strategies on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/GenomeStrategies.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/genome-strategies. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Planner: each fit strategy should open a pre-filled planner situation.
- Mechanisms: link each property strategy to its dossier.

### The Sequence Planner — `/portal/sequence-planner`

**Wiring 5.5/10 · Value 9/10 · Frequency 4/10**

*Right page when:* Any portfolio owner, or a first-time household with a goal.

**To reach ten — from the code:**
- No strategy in shared/genomeStrategies.ts names /portal/sequence-planner in relatedPaths — the predictive layer cannot route a household here.
- Only 1 other surface link to /portal/sequence-planner. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/sequence-planner on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/SequencePlanner.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/sequence-planner. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Genome: seed the situation from the genome and the fact finder.
- Zip: default appreciation from the zip series.
- Providers: name the lender whose threshold each stage used.

### The AI Brain Hub — `/portal/ai-brain-hub`

**Wiring 4.5/10 · Value 8/10 · Frequency 3/10**

*Right page when:* Advisors and the owner; what the channels know.

**To reach ten — from the code:**
- client/src/pages/AIBrainHubPage.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- No strategy in shared/genomeStrategies.ts names /portal/ai-brain-hub in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/ai-brain-hub. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/ai-brain-hub on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/AIBrainHubPage.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/ai-brain-hub. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Integration scorecard: link both ways; the hub should show unwired groups.

### The Career Ledger — `/portal/career-path`

**Wiring 4.5/10 · Value 8/10 · Frequency 4/10**

*Right page when:* Choosing or changing a professional path; physicians, dentists, vets, lawyers.

**To reach ten — from the code:**
- client/src/pages/CareerPathPage.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- No strategy in shared/genomeStrategies.ts names /portal/career-path in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/career-path. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/career-path on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/CareerPathPage.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/career-path. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Forgiveness: hand the loan balance across.
- Genome: link from the career factor.

### Forgiveness Engine — `/portal/forgiveness`

**Wiring 4.5/10 · Value 8/10 · Frequency 3/10**

*Right page when:* Federal student debt over $50k, or a household with a physician or public-service worker.

**To reach ten — from the code:**
- client/src/pages/portal/Forgiveness.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add shared/forgiveness.ts to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No strategy in shared/genomeStrategies.ts names /portal/forgiveness in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/forgiveness. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- No FigureTrace in shared/provenance.ts covers /portal/forgiveness. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Career ledger: hand the loan balance across.
- Live: studentaid.gov rule changes.

### The Inheritance Engine — `/portal/inheritance`

**Wiring 4.5/10 · Value 8/10 · Frequency 5/10**

*Right page when:* Expecting or planning an inheritance.

**To reach ten — from the code:**
- client/src/pages/portal/Inheritance.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add shared/inheritanceEngine.ts to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No strategy in shared/genomeStrategies.ts names /portal/inheritance in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/inheritance. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- No FigureTrace in shared/provenance.ts covers /portal/inheritance. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Erosion: already fed; expose the tax-odds source on the page.
- Household genome: step-family factors.

### IUL vs Roth — `/portal/iul-vs-roth`

**Wiring 4.5/10 · Value 8/10 · Frequency 5/10**

*Right page when:* Already maxing a Roth, or ineligible for one.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- No server/*.test.ts imports shared/ibbotsonModel.ts. Add one that reproduces a figure the page shows.
- Only 1 other surface link to /portal/iul-vs-roth. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/iul-vs-roth on the sphere in shared/sphere.ts.
- Calls trpc.iulVsRoth (server/iulVsRoth.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/iul-vs-roth. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Erosion: tax odds should drive the comparison.
- Genome: already linked.

### Real Estate Mogul — `/portal/real-estate-mogul`

**Wiring 4.5/10 · Value 8/10 · Frequency 3/10**

*Right page when:* Intending to own more than three doors.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- No server/*.test.ts imports shared/ibbotsonModel.ts. Add one that reproduces a figure the page shows.
- Only 1 other surface link to /portal/real-estate-mogul. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/real-estate-mogul on the sphere in shared/sphere.ts.
- Calls trpc.marketData (server/marketData.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/real-estate-mogul. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Planner: this IS a sequence; route the mogul model through it.
- Provenance: trace the equity line.

### Policy Loan Modeller — `/portal/policy-loans`

**Wiring 3.5/10 · Value 7/10 · Frequency 2/10**

*Right page when:* Cash value over $50k and a use for it.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- No server/*.test.ts imports shared/ibbotsonModel.ts. Add one that reproduces a figure the page shows.
- No strategy in shared/genomeStrategies.ts names /portal/policy-loans in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/policy-loans. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/policy-loans on the sphere in shared/sphere.ts.
- Calls trpc.policyLoanOptimizer (server/policyLoanOptimizer.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/policy-loans. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Mechanism: link to /portal/mechanism/policy-loan both ways.
- Thresholds: use policy-loan-value and the wash-loan variant.
- Planner: the policy:borrow stage should open this page.

### Tax-Free Income for Life — `/portal/income-for-life`

**Wiring 6/10 · Value 9/10 · Frequency 5/10**

*Right page when:* Within ten years of retirement, or any age with a Roth conversion decision pending.

**To reach ten — from the code:**
- client/src/pages/portal/IncomeForLife.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- No strategy in shared/genomeStrategies.ts names /portal/income-for-life in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/income-for-life. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- No FigureTrace in shared/provenance.ts covers /portal/income-for-life. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Planner: policy:borrow stage should cite this page's rate sheet.
- Genome: already linked; add the longevity factor as a gate.
- Live: SSA period life table refresh.

### Long-Term Care Planner — `/portal/long-term-care`

**Wiring 6/10 · Value 9/10 · Frequency 6/10**

*Right page when:* Fifty-five and older, or caring for a parent.

**To reach ten — from the code:**
- client/src/pages/portal/LongTermCare.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- No strategy in shared/genomeStrategies.ts names /portal/long-term-care in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/long-term-care. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- No FigureTrace in shared/provenance.ts covers /portal/long-term-care. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Longevity engine: joint survival odds.
- Zip: cost by zip is there; expose on the household page.
- Genome: link from the protection strategies.

### The Wealth Genome — `/portal/wealth-genome`

**Wiring 6/10 · Value 9/10 · Frequency 8/10**

*Right page when:* Everyone, once, before any strategy page.

**To reach ten — from the code:**
- client/src/pages/WealthGenomePage.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Only 0 other surfaces link to /portal/wealth-genome. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Calls trpc.factFinder (server/factFinderRouter.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/wealth-genome. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Already the predictive hub; add the household pairing prompt on completion.

### IUL Engine — `/portal/iul-engine`

**Wiring 5/10 · Value 8/10 · Frequency 4/10**

*Right page when:* Overfunding capacity for ten years and an accumulation goal beyond qualified plans.

**To reach ten — from the code:**
- client/src/pages/portal/IulEngine.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- No strategy in shared/genomeStrategies.ts names /portal/iul-engine in relatedPaths — the predictive layer cannot route a household here.
- Only 1 other surface link to /portal/iul-engine. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Calls trpc.iulLinks (server/iulLinksRouter.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/iul-engine. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Time machine: link historical crediting both ways.
- Mechanism dossier: cross-link policy-loan.
- Live: carrier cap/participation sheets dated.

### Policy Cost Lab — `/portal/policy-cost-lab`

**Wiring 5/10 · Value 8/10 · Frequency 3/10**

*Right page when:* Holding or considering a permanent policy and wanting the cost decomposed.

**To reach ten — from the code:**
- client/src/pages/portal/PolicyCostLab.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- No strategy in shared/genomeStrategies.ts names /portal/policy-cost-lab in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/policy-cost-lab. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Calls trpc.policyLab (server/policyLabRouter.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/policy-cost-lab. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Provenance: trace the cost-of-insurance line.
- Carrier registry: pull the product's charges from a dated source.

### Index Strategy Comparison — `/portal/index-strategies`

**Wiring 4/10 · Value 7/10 · Frequency 3/10**

*Right page when:* Choosing a crediting method on an indexed product.

**To reach ten — from the code:**
- client/src/pages/portal/IndexStrategyComparison.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Add shared/balancedIndexedAccount.ts to a group in shared/aiMemoryBank.ts so the twelve channels can use it.
- No strategy in shared/genomeStrategies.ts names /portal/index-strategies in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/index-strategies. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/index-strategies on the sphere in shared/sphere.ts.
- Calls trpc.marketData (server/marketData.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.

**Connections that would take it to ten — by judgement:**
- Already traced in provenance; add a genome link.

### QBI / Section 199A Optimizer — `/portal/qbi-optimizer`

**Wiring 6.5/10 · Value 9/10 · Frequency 4/10**

*Right page when:* Any pass-through business owner, especially specified-service trades near the phase-out.

**To reach ten — from the code:**
- No strategy in shared/genomeStrategies.ts names /portal/qbi-optimizer in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/qbi-optimizer. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/qbi-optimizer on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/QbiOptimizer.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.

**Connections that would take it to ten — by judgement:**
- Genome: link from the business-owner strategies.
- Live: annual inflation adjustment of the threshold.

### Wealth Genome Pairing Protocol — `/portal/household-genome`

**Wiring 5.5/10 · Value 8/10 · Frequency 5/10**

*Right page when:* Any couple with shared assets.

**To reach ten — from the code:**
- No strategy in shared/genomeStrategies.ts names /portal/household-genome in relatedPaths — the predictive layer cannot route a household here.
- Only 1 other surface link to /portal/household-genome. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/household-genome on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/HouseholdGenome.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/household-genome. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Planner: the primary-residence moves should show the veto weight.
- Consent: already gated; expose the record.

### Infinite Banking — `/portal/infinite-banking`

**Wiring 5.5/10 · Value 8/10 · Frequency 3/10**

*Right page when:* Anyone who has heard the phrase and wants the arithmetic.

**To reach ten — from the code:**
- No strategy in shared/genomeStrategies.ts names /portal/infinite-banking in relatedPaths — the predictive layer cannot route a household here.
- Only 1 other surface link to /portal/infinite-banking. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/infinite-banking on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/InfiniteBanking.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/infinite-banking. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Planner: link both ways.
- Thresholds: link the policy thresholds.

### The Thresholds — `/portal/thresholds`

**Wiring 5.5/10 · Value 8/10 · Frequency 3/10**

*Right page when:* Before signing anything with a lender or provider.

**To reach ten — from the code:**
- No strategy in shared/genomeStrategies.ts names /portal/thresholds in relatedPaths — the predictive layer cannot route a household here.
- Only 1 other surface link to /portal/thresholds. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/thresholds on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/Thresholds.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/thresholds. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Lender directory: each variant's providedBy should link a verified record.
- Freshness: 90-day re-verification sweep with a flag.

### MYGA Fixed Rates — `/portal/myga-fixed-rate`

**Wiring 4.5/10 · Value 7/10 · Frequency 5/10**

*Right page when:* A CD alternative decision, any age.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- No strategy in shared/genomeStrategies.ts names /portal/myga-fixed-rate in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/myga-fixed-rate. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/myga-fixed-rate on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/MYGAFixedRate.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/myga-fixed-rate. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Live: rates dated.
- Tested: mygaWaterfall.ts has no test.

### The Erosion Engine — `/portal/erosion`

**Wiring 7/10 · Value 9/10 · Frequency 7/10**

*Right page when:* Anyone with a tax-deferred balance and a horizon; the argument behind half the site.

**To reach ten — from the code:**
- client/src/pages/portal/Erosion.tsx imports no shared engine — the page's figures are not reproducible outside it. Move the arithmetic to shared/ and import it.
- Only 1 other surface link to /portal/erosion. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- No FigureTrace in shared/provenance.ts covers /portal/erosion. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Already the hub; add inbound links from every tax page's footer.

### Roth Conversion Ladder — `/portal/roth-conversion`

**Wiring 7/10 · Value 9/10 · Frequency 6/10**

*Right page when:* Pre-tax balance over $200k and a horizon past the conversion year.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- Calls trpc.clients (server/clients.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/roth-conversion. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Erosion: the power-adjusted tax odds should be the conversion's headline assumption.
- Planner: a conversion is a stage; expose it as a move.
- Provenance: trace the break-even year.

### Tax Waterfall — `/portal/tax-waterfall`

**Wiring 6/10 · Value 8/10 · Frequency 8/10**

*Right page when:* Any tax-paying household; the first tax page to read.

**To reach ten — from the code:**
- No strategy in shared/genomeStrategies.ts names /portal/tax-waterfall in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/tax-waterfall. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Calls trpc.taxBracketWaterfall (server/taxBracketWaterfall.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/tax-waterfall. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Provenance: trace one bracket figure.
- Genome: link from the tax strategies.

### Fixed Indexed Annuity Top Ten — `/portal/fia-top10`

**Wiring 5/10 · Value 7/10 · Frequency 4/10**

*Right page when:* Conservative appetite with a lump to place.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- No strategy in shared/genomeStrategies.ts names /portal/fia-top10 in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/fia-top10. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Calls trpc.marketData (server/marketData.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/fia-top10. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Live: rate sheets dated; the ten move monthly.
- Genome: link from the guaranteed-growth strategies.

### Household Wealth — `/portal/household-wealth`

**Wiring 5/10 · Value 7/10 · Frequency 6/10**

*Right page when:* Any household wanting the whole balance sheet on one page.

**To reach ten — from the code:**
- Name the engine in shared/calculatorCatalog.ts (engine: "shared/….ts") so provenance and the brain can cite it.
- No strategy in shared/genomeStrategies.ts names /portal/household-wealth in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/household-wealth. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Calls trpc.clients (server/clients.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/household-wealth. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Household genome: pair the spouses here.
- Test: householdWealth.ts has none.

### Alternative Lines of Credit for Rental Properties — `/portal/alt-credit`

**Wiring 6.5/10 · Value 8/10 · Frequency 3/10**

*Right page when:* Rental owner declined by, or done with, conventional lenders.

**To reach ten — from the code:**
- Only 1 other surface link to /portal/alt-credit. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/alt-credit on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/AltCreditHub.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/alt-credit. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Providers: the 39 dossier records should link into the directory.
- Thresholds: each lender's seasoning/LTV should appear on its record.

### How a Figure Gets Made — `/portal/how-a-figure-is-made`

**Wiring 6.5/10 · Value 8/10 · Frequency 4/10**

*Right page when:* Any client or advisor who wants to check a number.

**To reach ten — from the code:**
- Only 0 other surfaces link to /portal/how-a-figure-is-made. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/how-a-figure-is-made on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/HowAFigureIsMade.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/how-a-figure-is-made. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Three traces exist; every featured page should have one.

### Lifetime Guaranteed Income — `/portal/lifetime-income`

**Wiring 6.5/10 · Value 8/10 · Frequency 5/10**

*Right page when:* Guaranteed-income appetite above 6 on the genome, or a spouse with it.

**To reach ten — from the code:**
- Only 0 other surfaces link to /portal/lifetime-income. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/lifetime-income on the sphere in shared/sphere.ts.
- Calls trpc.lifetimeIncome (server/lifetimeIncome.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/lifetime-income. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Live: carrier payout-rate sheets on a dated sweep.
- Household genome: pair the two spouses' income-floor factors here.

### The Five Mechanisms — `/portal/mechanisms`

**Wiring 6.5/10 · Value 8/10 · Frequency 5/10**

*Right page when:* Any household choosing among capital mechanisms.

**To reach ten — from the code:**
- No strategy in shared/genomeStrategies.ts names /portal/mechanisms in relatedPaths — the predictive layer cannot route a household here.
- Place /portal/mechanisms on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/Mechanisms.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/mechanisms. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Genome: strategy-fit should rank the five by the household's factors.
- Sphere: place it.

### Liquidity Without the Bank — `/portal/liquidity-routes`

**Wiring 5.5/10 · Value 7/10 · Frequency 3/10**

*Right page when:* Needing capital without a bank underwriting event.

**To reach ten — from the code:**
- No strategy in shared/genomeStrategies.ts names /portal/liquidity-routes in relatedPaths — the predictive layer cannot route a household here.
- Only 1 other surface link to /portal/liquidity-routes. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/liquidity-routes on the sphere in shared/sphere.ts.
- No tRPC call from client/src/pages/portal/LiquidityRoutes.tsx — every figure is client-side and cannot be refreshed from a source. Add a router under server/ that supplies dated inputs.
- No FigureTrace in shared/provenance.ts covers /portal/liquidity-routes. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Alt-credit: merge or cross-link; they overlap.
- Genome: link from the liquidity factor.

### Retirement Income Projection — `/portal/retirement-projection`

**Wiring 5.5/10 · Value 7/10 · Frequency 8/10**

*Right page when:* Everyone; it is the first page most households should see.

**To reach ten — from the code:**
- No strategy in shared/genomeStrategies.ts names /portal/retirement-projection in relatedPaths — the predictive layer cannot route a household here.
- Only 0 other surfaces link to /portal/retirement-projection. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- Place /portal/retirement-projection on the sphere in shared/sphere.ts.
- Calls trpc.clientPortal (server/clientPortal.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/retirement-projection. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Engine: retirementDNA.ts is named but the page imports policyMechanics — reconcile.
- Brain: add retirementDNA.ts to a memory group.
- Test: none imports retirementDNA.

### The Zip Engine — `/portal/zip-engine`

**Wiring 8/10 · Value 9/10 · Frequency 5/10**

*Right page when:* Any property decision in a named zip.

**To reach ten — from the code:**
- No strategy in shared/genomeStrategies.ts names /portal/zip-engine in relatedPaths — the predictive layer cannot route a household here.
- No FigureTrace in shared/provenance.ts covers /portal/zip-engine. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Genome: link from the property strategies.
- Planner: appreciation input should default from the zip series.

### Mortgage Killer — `/portal/mortgage-killer`

**Wiring 7.5/10 · Value 8/10 · Frequency 7/10**

*Right page when:* Any mortgage and any surplus.

**To reach ten — from the code:**
- Place /portal/mortgage-killer on the sphere in shared/sphere.ts.
- Calls trpc.clients (server/clients.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.
- No FigureTrace in shared/provenance.ts covers /portal/mortgage-killer. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Mechanism: cross-link velocity-heloc dossier.
- Thresholds: heloc-cltv and the interest-calculation variant.
- Test: mortgageKiller.ts has none — the highest-traffic untested engine.

### The Mortgage Ledger — `/portal/mortgage-ledger`

**Wiring 8.5/10 · Value 8/10 · Frequency 6/10**

*Right page when:* Any mortgage the household wants to see by the dollar.

**To reach ten — from the code:**
- Place /portal/mortgage-ledger on the sphere in shared/sphere.ts.
- Calls trpc.mortgageKiller (server/mortgageKiller.ts) but that router fetches no external data source. Wire a sourced series (FRED, BLS, FHFA, Zillow) into it, or state on the page that the figures are static.

**Connections that would take it to ten — by judgement:**
- Planner: velocity stages should cite the ledger's interest line.

### Rental Enterprise — `/portal/rental-enterprise`

**Wiring 9/10 · Value 8/10 · Frequency 3/10**

*Right page when:* Five or more rentals, or the plan to get there.

**To reach ten — from the code:**
- No FigureTrace in shared/provenance.ts covers /portal/rental-enterprise. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- Planner: the blanket-refinance stage should hand the pool across.
- Thresholds: partial-release clause surfaced here.

### Short-Term Rentals — `/portal/short-term-rentals`

**Wiring 8/10 · Value 7/10 · Frequency 3/10**

*Right page when:* Considering nightly rental in a specific market.

**To reach ten — from the code:**
- Only 1 other surface link to /portal/short-term-rentals. Add it where a reader would need it next (a related page's footer, the relevant dossier, the planner).
- No FigureTrace in shared/provenance.ts covers /portal/short-term-rentals. Add one so a client can see how its headline figure is made.

**Connections that would take it to ten — by judgement:**
- STR tax page: link both ways.
- Genome: link.

## Every other catalogue page (unrated), by wiring

| Page | Wiring | Missing |
|---|---|---|
| Tax Bracket Visualizer `/portal/tax-brackets` | 6 | Genome routes here, Cross-linked, Live external data, Figure trace |
| Tax Schedule `/portal/tax-schedule` | 6 | Page uses an engine, Genome routes here, Cross-linked, Figure trace |
| The Integration Scorecard `/portal/integration-scorecard` | 5.5 | Page uses an engine, Genome routes here, Cross-linked, On the sphere, Figure trace |
| IUL Historical Performance `/portal/iul-historical` | 5.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| BRRRR with DSCR — How It Works `/portal/mechanism/brrrr-dscr` | 5.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Equity Share Agreements — How It Works `/portal/mechanism/equity-share` | 5.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Policy Loan Cycle — How It Works `/portal/mechanism/policy-loan` | 5.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Seller Financing and Wraps — How It Works `/portal/mechanism/seller-wrap` | 5.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Velocity Banking — How It Works `/portal/mechanism/velocity-heloc` | 5.5 | Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Premium Financing `/portal/premium-financing` | 5 | Engine declared, Genome routes here, Cross-linked, Live external data, Figure trace |
| Index Backtester `/portal/index-backtester` | 4.5 | Page uses an engine, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| The Physician's Edge `/portal/physicians-edge` | 4.5 | Page uses an engine, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Whisper Coach `/portal/whisper-coach` | 4.5 | Page uses an engine, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| FIA as Collateral `/portal/fia-collateral` | 3.5 | Engine declared, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Reverse HELOC `/portal/reverse-heloc` | 3.5 | Engine declared, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Tax-Advantaged Growth `/portal/tax-advantaged-growth` | 3.5 | Engine declared, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Goals-Based Planning `/portal/goals-planning` | 2.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Live external data, Figure trace |
| STR Strategy `/portal/str-strategy` | 2.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Live external data, Figure trace |
| Beneficiary Optimization `/portal/beneficiary-optimization` | 1.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Charitable Giving Optimizer `/portal/charitable-giving` | 1.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Income Gap Analyzer `/portal/income-gap` | 1.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Inflation Analysis `/portal/inflation` | 1.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Mortgage Killer III `/portal/mortgage-killer-v3` | 1.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Multi-Generational Transfer `/portal/multi-gen-wealth` | 1.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Policy Review `/portal/policy-review` | 1.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Risk Tolerance `/portal/risk-tolerance` | 1.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Succession Planning `/portal/succession-planning` | 1.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Tax-Loss Harvesting Scanner `/portal/tax-loss-harvesting` | 1.5 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, Live external data, Figure trace |
| Advisor Income Calculator `/portal/advisor-income-calculator` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Annuity Explorer `/portal/annuity-explorer` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Auto Closer `/portal/auto-closer` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Carrier Comparison `/portal/carrier-comparison` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Commission Calculator `/portal/commission-calculator` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Cryptocurrency Corner `/portal/crypto-corner` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Crypto Cycle `/portal/crypto-cycle` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Deal Room `/portal/deal-room` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Divorce Recovery `/portal/divorce-calculator` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Divorce & ILIT Strategy `/portal/divorce-ilit` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Divorce Recovery Planner `/portal/divorce-recovery` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Estate Document Generator `/portal/estate-document-gen` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Estate Flow Chart `/portal/estate-flow` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Financial Vitals `/portal/financial-vitals` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Hot Income `/portal/hot-income` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Income Annuity `/portal/income-annuity` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Income Timeline `/portal/income-timeline` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Interop Engine `/portal/interop-engine` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| IUL Projection `/portal/iul-projection` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Market Pulse `/portal/market-pulse` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Meeting Prep `/portal/meeting-prep` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| MYGA Waterfall `/portal/myga-waterfall` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Quick Quote `/portal/quick-quote` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Real Estate Desk `/portal/real-estate` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Retirement Spending Guardrails `/portal/retirement-guardrails` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Retirement Opportunities `/portal/retirement-opportunities` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Risk Score `/portal/risk-score` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Scenario Side by Side `/portal/scenario-side-by-side` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| The Strategy Lab `/portal/strategy` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Strategy Combinations `/portal/strategy-combos` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Strategy Compare `/portal/strategy-compare` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Tax Opportunity Detector `/portal/tax-opportunities` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Tax Return Reader `/portal/tax-return-upload` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Time Machine (AG-49) `/portal/time-machine-ag49` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |
| Trusts `/portal/trusts` | 1 | Engine declared, Page uses an engine, In the AI brain, Engine tested, Genome routes here, Cross-linked, On the sphere, Live external data, Figure trace |