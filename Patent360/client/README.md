# Patent360 client

Vite + React workbench. Deployed from this folder on Vercel.

```bash
npm install
npm run dev
npm run build
```

Live target: Vercel project `patent360` (team sam-russell), root directory `client`.
The Railway service in the repo root is the MCP hub, not this UI.
